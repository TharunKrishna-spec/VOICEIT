// FIREBASE DISABLED FOR LOCAL MODE
// This allows the app to function without a backend configuration.
// Data is saved to localStorage instead.

// Mock objects to prevent crash if imported
export const auth = {};
export const db = {};
export const app = {};
